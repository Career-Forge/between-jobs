"""Bundled reference data (small, generic, non-proprietary -- see
geo_country_aliases.json's own module docstring in geo_gazetteer.py for
why this is safe to commit directly, unlike the much larger city
gazetteer which is imported into the database, never git-tracked)."""
